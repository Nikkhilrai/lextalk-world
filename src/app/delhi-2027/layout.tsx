import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "LexTalk World Delhi 2027 — Coming Soon | LexTalk World",
    description: "LexTalk World returns to New Delhi in 2027. Dates, venue, and registration details will be announced soon.",
};

export default function Delhi2027Layout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
